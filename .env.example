# ABOUTME: Example environment variables for Liars Todo.
# ABOUTME: Copy to .env and fill in your values.

# Supabase Configuration
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Feature Flags
VITE_SHOW_LANDING_PAGE=false

# Push Notifications (generate with: npx web-push generate-vapid-keys)
VITE_VAPID_PUBLIC_KEY=your_vapid_public_key_here

# Note: These secrets should be set in Supabase dashboard, NOT in .env:
# - VAPID_PRIVATE_KEY: Private key for web push (generate with web-push)
# - RESEND_API_KEY: API key from resend.com for email notifications
